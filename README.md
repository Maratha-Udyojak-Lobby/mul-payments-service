# MUL Payments Service

Payment processing, UPI, cards, wallets, and transaction management.

## Development

```bash
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8005
```

Server runs on http://127.0.0.1:8005
